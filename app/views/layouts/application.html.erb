<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Rent a Rembrandt</title>
    <%= favicon_link_tag asset_path('icon-favicon.png') %>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
  <body>
    <div id="navbar-landing">
      <a id="icon-home" href="<%= root_path %>">
        <i class="fas fa-palette"></i>
      </a>

      <div id="user-actions">

          <% if user_signed_in? %>
            <a href="<%= new_painting_path %>" class="btn btn-main-customizable">Add Painting</a>
            <a href="<%= destroy_user_session_path %>" data-method="delete" class="btn logout-button">Logout</a>
            <a href="<%= dashboard_path %>"><img class="avatar dropdown-toggle" aria-haspopup="true" aria-expanded="false" src="https://kitt.lewagon.com/placeholder/users/ssaunier" /></a>
          <% else %>
            <a href="<%= new_user_session_path %>" class="btn login-button">Sign In</a>
            <a href="<%= new_user_registration_path %>" class="btn signup-button">Sign Up</a>
          <% end %>

      </div>
    </div>

    <%= yield %>

      <div class="footer mt-3 justify-content-center border-top">
        <div class="footer-links">
          <a href="#" style="font-size: 30px;"><i class="fab fa-github px-2"></i></a>
          <a href="#" style="font-size: 30px;"><i class="fab fa-instagram px-2"></i></a>
          <a href="#" style="font-size: 30px;"><i class="fab fa-facebook px-2"></i></a>
          <a href="#" style="font-size: 30px;"><i class="fab fa-twitter px-2"></i></a>
          <a href="#" style="font-size: 30px;"><i class="fab fa-linkedin px-2"></i></a>
        </div>
      </div>
    </div>

    <%= javascript_include_tag 'application' %>

    <%= javascript_pack_tag 'application' %>
  </body>
</html>
