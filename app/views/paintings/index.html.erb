<div class="d-flex justify-content-center">
<h1> <strong>All Paintings </strong> </h1>
</div>

<div class="full-card">

  <% @paintings.each do |painting| %>
    <%= link_to (render 'painting-card', painting: painting), painting_path(painting), :style=>'color: black; text-decoration: none' %>
  <% end %>

</div>


