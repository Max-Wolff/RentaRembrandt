<div class="show-view">

  <% unless @painting.photo.attached? == false %>
    <%= cl_image_tag @painting.photo.key, height: 300, width: 400, crop: :fill, class: "show-image" %>
  <% else %>
    <img class="show-image" src="https://p5.focus.de/img/fotos/crop3263390/0685332128-w1200-h627-q75-p5/Ausstellungen-Das-Bild-Der-Schrei-von-Edvard-Munch.jpg" />
  <% end %>

  <div class="show-info">

    <div class="painting-info">

      <div class="info-title">

        <h2 class="painting-title">"<%= @painting.title %>" by <%= @painting.artist %></h2>
        <% @owner = User.find(@painting.user_id) %>

        <div class="owner-info">

          <% unless @owner.photo.attached? == false %>
            <%= cl_image_tag @owner.photo.key, height: 400, width: 400, crop: :fill, alt: "avatar", class: "card-trip-user avatar-bordered" %>
          <% else %>
            <img class="card-trip-user avatar-bordered" src="https://image.flaticon.com/icons/svg/21/21104.svg" style="background-color: white;" />
          <% end %>
          <p><%= @owner.fullname %></p>

        </div>

      </div>

      <p>This is the image Description Lorem ipsum dolor sit amet, consectetur adipisicing elit. Id sint ab molestiae, repellat consectetur odit itaque? Suscipit quia sit alias dolor voluptatem ipsa expedita nam porro assumenda id, tempore fugit.</p>
      <p>Place for Reviews</p>
      <p>Review 1</p>
      <p>Review 2</p>
      <p>Review 3</p>
    </div>

    <div class="booking-form">
      <p class="booking-pricing"><%= @painting.price_per_day %>€ / Day</p>

      <div class="widget-content" data-unavailable="<%= @painting.unavailable_dates.to_json %>"></div>
      <%= simple_form_for([@painting, Booking.new]) do |f| %>
        <%= f.error_notification %>
        <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

        <div class="form-inputs">
          <%= f.input :start_date, as: :string, label: "Dates", placeholder: "From", wrapper_html: { class: "inline_field_wrapper" } %>
          <%= f.input :end_date, as: :string, label: '&nbsp;'.html_safe, placeholder: "To", disabled: true, wrapper_html: { class: "inline_field_wrapper" } %>
        </div>

        <div class="booking-calculation">
          <p><%= @painting.price_per_day %> x 2 Days</p>
          <p><%= @painting.price_per_day * 2 %>€</p>
        </div>

        <div class="form-actions">
          <%= f.submit "Request", class: "btn btn-primary btn-lg btn-block btn-booking" %>
        </div>
      <% end %>

    </div>


  </div>
</div>

<% if current_user == @owner %>
 <%= link_to "Edit your Painting", edit_painting_path(@painting) %>
<% end %>
