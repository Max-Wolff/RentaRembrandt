<div id="navbar-landing">
  <a id="icon-home" href="<%= root_path %>">
    <i class="fas fa-palette"></i>
  </a>

  <div id="user-actions">

      <% if user_signed_in? %>
        <a href="<%= new_painting_path %>" class="btn btn-light">New Painting</a>
        <a href="<%= destroy_user_session_path %>" data-method="delete" class="btn btn-danger">Logout</a>
        <a href="<%= dashboard_path %>"><img class="avatar dropdown-toggle" aria-haspopup="true" aria-expanded="false" src="https://kitt.lewagon.com/placeholder/users/ssaunier" /></a>
      <% else %>
        <a href="<%= new_user_session_path %>" class="btn btn-success">Log In</a>
        <a href="<%= new_user_registration_path %>" class="btn btn-primary">Sign Up</a>
      <% end %>

  </div>
</div>
